version: 2

project_name: agentcookie

before:
  hooks:
    - go mod tidy

builds:
  # Darwin build: runs on macos-latest, codesigned
  - id: agentcookie-darwin
    main: ./cmd/agentcookie
    binary: agentcookie
    env:
      - CGO_ENABLED=1
    goos:
      - darwin
    goarch:
      - arm64
      - amd64
    ldflags:
      - -s -w -X github.com/mvanhorn/agentcookie/internal/cli.Version={{ .Env.AGENTCOOKIE_RELEASE_VERSION }}
    hooks:
      post:
        - cmd: scripts/prepare-darwin-release-binary.sh {{ .Path }}
          output: true

archives:
  - id: agentcookie
    name_template: "{{ .ProjectName }}_{{ .Env.AGENTCOOKIE_RELEASE_VERSION }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md
      - skill/SKILL.md
      - skill/prompts/install-on-both-machines.md
      - scripts/install-beta.sh
      - docs/agent-browser-sessions.md
      - docs/fork/POLICY.md
      - docs/fork/RELEASING.md
      - docs/fork/generations/v1.1.0.md
      - docs/threat-model.md
      - docs/architecture.md
      - docs/protocol.md
      - docs/faq.md
      - examples/source.yaml
      - examples/sink.yaml
      - examples/blocklist.yaml
      - examples/launchd-sink.plist

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - merge
